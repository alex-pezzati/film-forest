extends layout.pug

block content
    .container
        
        .cell.cell-2
            ul
                div My Movies
                div Watched
                div Want to watch
        .cell.cell-3 
            table.GeneratedTable.sortable.myTable
                thead
                    tr
                        th Poster
                        th Title
                        th Average Rating
                        th Genre
                        th Edit
                        th Delete
                tbody
                    each movie in movies
                        each savedMovie in myDashboardMovies
                            if movie.id===savedMovie.movieId
                                tr
                                    td: a(href=`/movies/${movie.id}`)
                                        img(src=`${movie.movieUrl}` height='100px')
                                    td.title: a(href=`/movies/${movie.id}`)= movie.title
                                    td
                                        if movie.totalRating > .70
                                            span.fire=  '🔥'
                                        if movie.totalRating < .69
                                            span.Disaster= '🌪'
                                        span= Math.floor(movie.totalRating * 100)+'%'
                                    td.genre= movie.genre
                                                table.GeneratedTable.sortable
        

            



//- thead
//-     tr
//-         th Poster
//-         th Title
//-         th Average Rating
//-         th Genre
//- tbody
//-     each movie in movies
//-         tr
//-             td: a(href=`/movies/${movie.id}`)
//-                 img(src=`${movie.movieUrl}` height='100px')
//-             td: a(href=`/movies/${movie.id}`)= movie.title
//-             td
//-                 if movie.totalRating > .70
//-                     span.fire=  '🔥'
//-                 if movie.totalRating < .69
//-                     span.Disaster= '🌪'
//-                 span= Math.floor(movie.totalRating * 100)+'%'
//-             td= movie.genre
//- table.GeneratedTable.sortable
//-     thead
//-         tr: th.tableHead(colspan = 3) The Best Movies
//-         tr
//-             td Poster
//-             td Title
//-             td Average Rating
//-     tbody.tableBody
//-         each movie in moviesBest
//-             tr
//-                 td: a(href=`/movies/${movie.id}`)
//-                     img(src=`${movie.movieUrl}` width='100%')
//-                 td: a(href=`/movies/${movie.id}`)= movie.title
//-                 td
//-                     if movie.totalRating > .70
//-                         span.fire=  '🔥 '
//-                     if movie.totalRating < .69
//-                         span.Disaster= '🌪 '
//-                     span= Math.floor(movie.totalRating * 100)+'%'