doctype html
html
  head
      block head
        title= "Film Forest"
        link(rel='stylesheet' href='/stylesheets/primary.css')
  nav
        if locals.authenticated
          form(action='/users/logout' method='post')
            button(type='submit') Logout
        else
          h1 Welcome to Film Forest
          button(type='submit' href='/users/login' ) Login
          button(href='/users/register') Register
  //- body
  //-   nav
  //-     div
  //-       ul
  //-         if locals.authenticated
  //-           form(action='/users/logout' method='post')
  //-             button(type='submit') Logout
  //-         else
  //-           span
  //-             a(href='/users/login') Login
  //-             a(href='/users/register') Register
  block content
